/* If statement */

foo := if a == 1 then bar
else baz
"What is this about?";

/* Multiline statements */

y := u1 > 0
and u2 > 0 and
u3 > 0
        and u4 > 0;

Modelica.Utilities.Streams.print(
    "foo" + "bar"
        + "baz");
